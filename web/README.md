# Web

Future page-2-code client application.

The client direction is React Native with NativeWind. Frontend implementation is intentionally deferred while the backend foundation is built.

## Development container

The web container starts Bash and mounts the complete repository at `/app`.

```bash
podman exec -it page-2-code-web bash
```
